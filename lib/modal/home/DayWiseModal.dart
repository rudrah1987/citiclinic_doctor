class DayWiseModal{
  String day;
  int dayValue;

  DayWiseModal(this.day, this.dayValue);
}

class SessionWiseModal{
  String session;
  int sessionValue;

  SessionWiseModal(this.session, this.sessionValue);
}